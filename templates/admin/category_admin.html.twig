{% extends 'admin/inner_nav.html.twig' %}

{% block actionButton %}
    <a href="{{ path('category_create') }}" class="btn btn-success" data-modal="CategoryForm" >
        Create <i class="fa-solid fa-circle-plus"></i></a>
    <a href="{{ path('category_create') }}" class="btn btn-success d-none">Create
            <i class="fa-solid fa-circle-plus"></i></a>
{% endblock %}

{% block innerCardBody %}
    <div>
        <div class="col-12" id="categoryTable">
            {{ include('tables/category_table.html.twig') }}
        </div>
    </div>
{% endblock %}

