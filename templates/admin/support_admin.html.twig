{% extends 'admin/inner_nav.html.twig' %}
{% block actionButton %}
    <a href="{{ path('contact') }}" class="btn btn-success" data-modal="ContactForm">
        Create <i class="fa-solid fa-circle-plus"></i></a>
    <a href="{{ path('contact') }}" class="btn btn-success d-none">Create<i
            class="fa-solid fa-circle-plus"></i></a>
{% endblock %}
{% block innerCardBody %}
    <div>
        <div class="col-12" id="requestTable">
            {{ include('tables/request_table.html.twig') }}
        </div>
    </div>
{% endblock %}
