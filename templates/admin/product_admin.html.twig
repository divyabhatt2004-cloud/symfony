{% extends 'admin/inner_nav.html.twig' %}

{% block actionButton %}
        <a href="{{ path('product_create') }}" class="btn btn-success" data-modal="productForm" >
            Create <i class="fa-solid fa-circle-plus"></i></a>

    <a href="{{ path('product_create') }}" class="btn btn-success d-none">Create
            <i class="fa-solid fa-circle-plus"></i></a>
{% endblock %}

{% block innerCardBody %}
    <div class="row">
        <div class="col-12">
           {{ include('tables/product_table.html.twig') }}
        </div>
    </div>
{% endblock %}
