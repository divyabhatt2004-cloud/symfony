{% extends 'admin/inner_nav.html.twig' %}

{% block actionButton %}
    <button class="btn btn-success"><a href="{{ path('product_create') }}" class="text-white text-decoration-none">Create
            <i class="fa-solid fa-circle-plus"></i></a></button>
{% endblock %}

{% block innerCardBody %}
    <div class="row">
        <div class="col-12">
           {{ include('tables/product_table.html.twig') }}
        </div>
    </div>
{% endblock %}
